I used an AI coding agent (ChatGPT / Genie) to:

- Generate the initial SQL views for KPI computation
- Draft the Streamlit app skeleton
- Improve the README and architecture documentation

Example:
Prompt: "Generate SQL to compute CTR, CVR and ROI from campaign table"
Result: used in sql/kpi_views.sql

I validated the outputs by running them in Fabric / Databricks and checking KPI tables (see screenshots/kpi_table.PNG).
